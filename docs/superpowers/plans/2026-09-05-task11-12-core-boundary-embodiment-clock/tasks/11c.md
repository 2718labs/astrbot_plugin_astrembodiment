# 11C Host Cutover to Core Ingress and EmbodimentClock

Owner: host-clock-owner
Depends on: 11B

## Goal

Move the plugin Host to the typed core lanes and one adaptive persona clock
while retaining ordinary replies, Genesis and inbound semantic appraisal.

## Context

Read only:

- [core-ingress-delivery.md](../contracts/core-ingress-delivery.md)
- [rolling-embodiment-clock.md](../contracts/rolling-embodiment-clock.md)
- accepted 11B API and import evidence

The clock has no Provider callback, externalization callback, target store or
send adapter.

## Write Scope

- `main.py`
- `_conf_schema.json`
- `astr_embodiment/__init__.py`
- `astr_embodiment/bridge.py`
- `astr_embodiment/coordinator.py`
- `astr_embodiment/interaction.py`
- `astr_embodiment/embodiment_clock.py`
- `tests/test_embodiment_clock.py`
- `tests/test_ordinary_reply_flow.py`
- `tests/test_semantic_appraisal_core.py`
- `tests/test_persona_genesis_compatibility.py`

Accepted minimal Host codec extension (2026-09-08):

- `crates/ae-pyo3/src/lib.rs`
- `python/astrembodiment_core/__init__.py`

`compile_core_host_request_v1(operation, request_json)` is a pure typed request
compiler for `inbound`, `delivery`, `create`, and `profile_cas`. It reuses the
frozen Native BLAKE3/structural codec to fill deterministic IDs. It cannot open
storage, acquire CORE, read time, commit, call Provider, or grant execution
authority. This avoids an online Python hashing dependency or duplicate codec.
12A/12B must include this symbol in the final public API/package allowlist and
rebuild both platform wheels; a raw maturin wheel still needs the existing
content-addressed `_bundled` manifest layout before Host import.

Do not delete retired modules in this card; 12A owns atomic removal.
Do not add or alter a v9 table, index or trigger; 11A installed the complete
schema and 11B exposed its typed methods.

## Steps

1. Replace supervisor startup/shutdown with one min-heap EmbodimentClock and
   authenticated inventory rescan.
2. Create one asyncio lock per exact bot/persona and enforce the frozen
   Host -> PyO3 -> SQLite -> hot-state order without heap/Provider await inside.
3. Commit inbound first, freeze _pending against its durable operation while
   still locked, release for optional Provider, and reacquire for settlement.
4. Commit the ordinary DeliveryOutcome through the typed API and remove the
   GenesisCoordinator._applied shortcut; clear exact _pending on every terminal
   delivery result.
5. Capture one frozen persona time, use Native status first and treat 5m/1h/6h
   as polling ceilings. Run advance only with the exact Due request bytes
   Native returned for a boundary or analytic horizon.
6. Show only clock enabled, persona timezone/sleep and semantic budget. Preserve
   old config bytes as ignored and never write them back.
7. Keep Genesis, semantic attempts/reservations, matrix context and ordinary
   AstrBot response behavior live.

## Acceptance

Run only:

- python -m pytest tests/test_embodiment_clock.py tests/test_ordinary_reply_flow.py tests/test_semantic_appraisal_core.py tests/test_persona_genesis_compatibility.py
- python -m compileall main.py astr_embodiment
- git diff --check

Pass requires one bounded restart advance, zero-write NotDue, delivery after an
intervening clock commit, _pending survival across Provider/settle failure,
clean cancellation, and zero Provider/Token/network/send/target/relation calls
from idle, active, asleep and error clock paths.

## Return

Return commit SHA, changed paths, lifecycle trace, focused output and blockers.
Do not call a mocked scheduler proof a real Native integration pass.

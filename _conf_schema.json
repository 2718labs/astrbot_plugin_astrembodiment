{
  "runtime_envelope": {
    "description": "运行资源档位",
    "hint": "只影响运行时资源包络，不改变身份公式或行为。",
    "type": "string",
    "options": ["auto", "reference-2c2g-v1", "compat-1c1g-v1"],
    "labels": ["自动", "参考资源档位（2 核 2G）", "兼容资源档位（1 核 1G）"],
    "default": "auto"
  },
  "native_data_dir": {
    "description": "原生核心数据目录",
    "hint": "可填写持久化宿主目录的绝对路径；留空时使用 AstrBot 为插件分配的数据目录。普通插件更新会继续使用同一目录中的 Store/Vault，不会因插件版本变更初始化身份。",
    "type": "string",
    "default": ""
  },
  "continuity_vault_dir": {
    "description": "人格连续性 Vault 目录",
    "hint": "可选。仅接受绝对路径且末级目录必须为 continuity-vault；用于明确复用既有连续性 Vault。留空时沿用原生核心数据目录。不要把此项用于新建身份；重生或清空只能由用户完成两步确认。",
    "type": "string",
    "default": ""
  },
  "observatory_enabled": {
    "description": "启用只读诊断",
    "hint": "启用时成功运算输出一行中文简洁摘要；失败始终输出完整警告。日志不包含消息正文、身份标识、拓扑或内部神经状态。",
    "type": "bool",
    "default": true
  },
  "node_observability_detailed_logging": {
    "description": "启用详细运行观测日志",
    "hint": "仅原生布尔值 true 启用固定语义注入 JSON；它覆盖“启用只读诊断”的成功日志开关，失败警告无论开关均会保留。详细日志只含聚合标量，不含节点/边数组、端点、权重、拓扑或内部神经状态。",
    "type": "bool",
    "default": false
  },
  "proactive_enabled": {
    "description": "允许提出主动联系建议",
    "hint": "实际发送仍由 AstrBot 和用户控制；MVP 默认关闭。",
    "type": "bool",
    "default": false
  },
  "model_settings": {
    "description": "模型辅助设置",
    "type": "object",
    "items": {
      "assistant_provider_id": {
        "description": "辅助模型 Provider",
        "hint": "统一用于辅助能力与当前请求的闭合 15 维语义估计；留空时使用当前会话 Provider。",
        "type": "string",
        "_special": "select_provider",
        "default": ""
      },
      "semantic_estimator_timeout_ms": {
        "description": "V3 语义估计超时（毫秒）",
        "hint": "超时后不提交语义、不回退固定零，并记录 EXPRESSION_NOT_ATTEMPTED 警告。",
        "type": "int",
        "default": 8000,
        "minimum": 1000,
        "maximum": 15000
      }
    },
    "default": {}
  },
  "seed_code": {
    "description": "SeedCode（身份种子）",
    "hint": "系统生成，仅用于查看；真实身份与连续状态由 durable Store/Vault 保存。重载或插件更新不会以 SeedCode 重新初始化；只有用户明确发起并确认的重生/清空动作才会创建新 incarnation。",
    "type": "text",
    "default": "",
    "readonly": true
  }
}
